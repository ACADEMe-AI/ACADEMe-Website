# Lottie FX (non-mascot)

Use Lottie for **generic UI effects only**. Character face/body = **Rive `mee.riv`**.

| Planned file | Use |
|--------------|-----|
| `check-pop.json` | Correct answer |
| `confetti-success.json` | Optional under big celebrate |

## Flutter

```yaml
dependencies:
  lottie: # pin stable
```

Recolor exports to Scheme B/D tokens before shipping.

## Free sources (recolor + license check)

- LottieFiles (filter free + license)
- Create simple check in Rive and export if needed

Do **not** put Mee face animations here as separate Lotties.
